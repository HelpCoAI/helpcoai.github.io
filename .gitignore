
# crawler page cache
grants/cache/
grants/data/.enrich_cache/

# site build artifacts. dist/ is regenerated by `npm run build` from
# awards.json, which IS committed -- so a fresh clone can rebuild the whole
# site without re-running the crawl or paying for enrichment again.
grants/site/node_modules/
grants/site/dist/
grants/site/.astro/
